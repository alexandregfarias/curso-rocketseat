// Criando objeto com valores e propriedades: 
const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true
}

// a pessoa John tem 30 anos e pesa 88.6.

console.log(`$ A pessoa ${person.name} tem ${person.age} ano e pesa ${person.weight}.`)

// Ela é admin do sistema?

console.log(person.isAdmin == true)